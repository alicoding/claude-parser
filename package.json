{
  "name": "@claude-parser/monorepo",
  "version": "2.0.0",
  "private": true,
  "description": "TypeScript SDK for parsing Claude Code JSONL files",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "clean": "npm run clean --workspaces",
    "dev": "npm run dev --workspaces",
    "bundle-analysis": "npm run bundle-analysis --workspaces"
  },
  "dependencies": {
    "ky": "^1.7.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Claude Parser Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/anthropics/claude-parser.git"
  },
  "keywords": [
    "claude",
    "jsonl",
    "parser",
    "typescript",
    "react",
    "hooks",
    "claude-code"
  ]
}